<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >


    <TextView
        android:id="@+id/pendingUsernameView"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:layout_marginTop="8dp"
        android:text="Username"
        android:textSize="24sp"
        app:layout_constraintLeft_toRightOf="@+id/pending_image_message_profile"
        app:layout_constraintRight_toLeftOf="@id/acceptFriendBtn"
        app:layout_constraintTop_toTopOf="@+id/pending_image_message_profile" />

    <ImageView
        android:id="@+id/pending_image_message_profile"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:background="@drawable/ic_empty"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_margin="8dp"
        app:layout_constraintLeft_toLeftOf="parent" />

    <Button
        android:id="@+id/acceptFriendBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:layout_marginLeft="12dp"
        android:background="@color/colorPrimary"
        android:text="Accept"
        app:layout_constraintLeft_toRightOf="@id/pendingUsernameView"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="parent"
        tools:layout_editor_absoluteY="8dp" />

</android.support.constraint.ConstraintLayout>
